[pytest]
# В окружении есть несовместимый сторонний плагин pytest_ethereum (web3),
# который падает на импорте и роняет весь прогон до сбора тестов. Блокируем его
# по имени, как это сделано в трёх legacy-проектах.
addopts = -p no:pytest_ethereum
testpaths = tests
