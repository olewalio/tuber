# ТЗ-37. Сводка 08:00: убрать смешение шкал и не-ИИ материал из «Виральных»

Дата: 18.09.2026. Модуль: `tuber/analysis/report.py`. Исполнитель: jCode (`--local`).
База: коммит `7ce4ec0` (ТЗ-36). Спеки: `docs/TZ-35-viral-by-audience.md`, `docs/TZ-36-viral-freshness-and-topics.md`.

## 1. Фактическая выдача сводки после ТЗ-36 (мой прогон на копии боевой базы, 18.09.2026)

Сводка собрана, размер 3 835 байт (лимит 4 000), размер в норме. Состав раздела «Виральные — охват
против своей аудитории» (7 позиций):

1. Завод TerraFab утроит выпуск чипов — Rayan Miller; охват 438.15; 8 дн (блок 1);
2. Робот оказывает медицинскую помощь паломнику — Robot services; охват 304.53; 9 дн (блок 1);
3. Разработчики в 2026 — LearnCodez; охват 248.09; 5 дн (блок 1);
4. Unitree открывает исходный код UnifoLM-WLA-1.0 — Unitree Robotics; **охват 1.88**; 4 151 235 просмотров (блок 3.1);
5. GPT-6 Astra — конец разработчиков? — CodeWithHarry; **охват 0.35**; 3 522 093 просмотра (блок 3.1);
6. @AnthropicAI: threat intelligence report (блок 4);
7. `[vot_tak_exclusive]` «Наручники наденем, током будем бить» — о похищении девушки в приграничье (блок 5).

Дефект 1. Позиции 4 и 5 противоречат названию раздела: «Виральные — охват против своей аудитории» с
охватом 0.35 и 1.88 — это не виральность, а крупные каналы с абсолютным охватом. Причина: в сводку
берутся позиции блока 3.1, у которого пол — абсолютные просмотры ≥ 50 000, без проверки охвата.

Дефект 2. Позиция 7 — не ИИ-материал (похищение человека). Причина: у Telegram-материалов
`classification` пуст (замер: `select count(*) from classification ... platform='telegram'` = 0),
тематического фильтра нет, а сам канал в корпусе присутствует. Материал проходит единственный пол
блока 5 — «просмотры ≥ 3× нормы канала».

Обоснование правок — фактическая замена (замер на копии боевой базы, свежие, просмотров ≥ 50 000,
охват ≥ 3, продуктовые темы):

| материал | канал | подп | просмотры | охват | дней |
|---|---|---|---|---|---|
| GPT-6 Astra Use Cases Are Getting Ridiculous | Explaining AI | 290 | 526 935 | 1817 | 9 |
| ИИ, который пришлось ОТКЛЮЧИТЬ | АйтИИшник | 264 | 177 509 | 672 | 5 |
| GPT-6 Astra is Here... And It's Terrifying! | Kshitij Explains | 168 | 108 122 | 643 | 9 |
| Kal Aa Raha Hai iOS 27! Siri AI + New Features | Tech With Shekhar | 196 | 96 821 | 493 | 4 |
| Startup Revolution AI Summit 2026 (трейлер) | Startup Revolution AI | 1 360 | 99 422 | 73 | 7 |

Это ровно тот класс материала, который нужен владельцу: новая фича/релиз у канала с крошечной базой
подписчиков и большим абсолютным охватом. Замену брать из фактических данных, а не выдумывать.

## 2. Что сделать

### 2.1. Инвариант «Виральных» в сводке

- В сводке раздел «Виральные» формируется ТОЛЬКО из YouTube-позиций с охватом на подписчика ≥ 3.0.
- Состав: до 3 позиций блока 1 (полезное) + до 2 позиций блока 3.1 с двойным полом (просмотров ≥ 50 000
  И охват ≥ 3.0) + до 1 позиции блока 4 (X — истории с реакциями).
- Дедупликация по `content_id` (позиция, уже взятая из блока 1, не повторяется из блока 3.1).
- Пометка источника в строке сводки: у позиций блока 3.1 — `(продукт)`, у позиций блока 4 — `(X)`.
  У позиций блока 1 пометки нет.
- В строке сводки для позиций блока 3.1 печатать охват на подписчика и возраст материала.

### 2.2. Telegram (блок 5) в сводку не пускать

- Блок 5 остаётся в полном отчёте без изменений.
- В сводке Telegram-позиций нет вообще, пока у Telegram-материалов нет тематической маркировки.
  Причина письменно зафиксирована в техдолге (п. 2.4), а не только в этом ТЗ.

### 2.3. Инвариант нельзя обойти при нехватке позиций

- Если позиций блока 3.1 с охватом ≥ 3.0 меньше двух, сводка показывает меньше — НЕ добирать позициями
  с охватом < 3.0 и не подставлять абсолютные просмотры. Лучше меньше строк, чем строка с охватом 0.35.
- Если позиций блока 1 меньше трёх — показывать сколько есть, без добора.

### 2.4. Техдолг D-52 (связка маркер ↔ реестр обязательна)

- В `tuber/analysis/report.py` в месте, где Telegram исключён из сводки, поставить маркер:
  `# TODO(debt-D-52): Telegram-блок без тематической маркировки — в сводку не пускать (см. TECH-DEBT.md)`
- В `TECH-DEBT.md` (корень репозитория) добавить строку таблицы с ID `D-52`:
  место — `tuber/analysis/report.py` (`viral_compact_items`); суть — у Telegram-материалов
  `classification` пуст, поэтому блок «Telegram выше нормы канала» пропускает не-ИИ материалы
  (факт 18.09.2026: `vot_tak_exclusive` — пост о похищении человека попал в сводку 08:00);
  статус — **открыт**; что сделать — пометить тематику Telegram-каналов (не постов) и фильтровать
  блок 5 по ней, после чего вернуть 1 позицию Telegram в сводку.
- Проверить связку тестом `tests/test_debt_markers.py` (он двусторонний).

## 3. Границы работы

- Менять только `tuber/analysis/report.py`, тесты и `TECH-DEBT.md`. Секции 1–4 полного отчёта и блоки
  1–5 полного отчёта НЕ трогать: их состав и строки должны остаться такими же, как на коммите `7ce4ec0`
  (проверить сверкой на одной и той же копии базы, нормируя только time-dependent поля).
- Боевую базу `data/tuber.db` и `data/.lock` не трогать, чтение — `store.open_readonly`, выдача — на копии
  (`VACUUM INTO /tmp/tz37-copy.db`).
- Никаких новых зависимостей и сетевых вызовов.

## 4. Тесты (дополнить `tests/analysis/test_report_viral.py`, не менее 8 новых)

1. Позиция блока 3.1 с охватом 0.35 (просмотры огромные) в сводку НЕ попадает, а в полном отчёте остаётся.
2. Позиция блока 3.1 с охватом ≥ 3.0 в сводку попадает и помечена `(продукт)`, в строке есть охват и возраст.
3. Telegram-позиция (блок 5) в сводку не попадает, в полном отчёте присутствует.
4. Инвариант: ни одна YouTube-позиция раздела «Виральные» в сводке не имеет охват < 3.0 (проверка по
   всем строкам сводки, парсингом числа охвата).
5. Нехватка позиций: если кандидатов блока 3.1 с охватом ≥ 3.0 нет — в сводке только позиции блока 1 и X,
   добора «абсолютными» позициями нет.
6. Дедупликация: материал, взятый в блок 1, не повторяется как `(продукт)`.
7. Пометки `(продукт)` / `(X)` присутствуют ровно там, где нужно, и не появляются у позиций блока 1.
8. `tests/test_debt_markers.py` проходит с новой строкой D-52.

## 5. Приёмка (фактический текст, не обещание)

1. Полный текст сводки и её размер в байтах; размер ≤ 4 000.
2. Список позиций «Виральных» с охватом каждой; подтверждение, что все YouTube-позиции имеют охват ≥ 3.0.
3. Отметки ЕСТЬ/НЕТ по `Unitree`, `CodeWithHarry`, `vot_tak_exclusive`: в сводке — НЕТ, в полном отчёте — ЕСТЬ.
4. Числа по блокам полного отчёта (рассмотрено / прошло полы / показано) и подтверждение, что строки блоков
   1–5 полного отчёта совпадают с выдачей `7ce4ec0` на той же копии.
5. `python3 -m pytest -q tests/` — последние строки; известное падение
   `tests/x/test_tz6.py::test_report_translates_and_caches_in_report_texts` (воспроизводится на `1a05fb6`)
   перечислить отдельно.
6. Коммит:
   `ТЗ-37: сводка — в «Виральные» только охват ≥ 3, Telegram исключён (D-52)`
