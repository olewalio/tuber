# ТЗ-38. Сводка 08:00: убрать Telegram-раздел целиком, пока нет маркировки

Дата: 18.09.2026. Модуль: `tuber/analysis/report.py` (`build_compact` и разделы сводки).
База: коммит `2f6b933` (ТЗ-37). Спеки: `docs/TZ-35-viral-by-audience.md`, `docs/TZ-36-viral-freshness-and-topics.md`,
`docs/TZ-37-compact-viral-invariants.md`.

## 1. Что осталось

ТЗ-37 убрало Telegram только из раздела «Виральные» (`viral_compact_items`), но в сводке остался
отдельный раздел **«3. Telegram — топ постов за окно»**, который читает `telegram_section` без
тематического фильтра. Фактическая выдача сводки 18.09.2026 (моя проверка на копии боевой базы,
коммит `2f6b933`, размер 3 829 байт):

```
3. Telegram — топ постов за окно:
1. [CGIT_Vines] Я тут на радостях с коннектомами решил покопать дорожную карту… главный следующий
   рубеж — мышь… — significance 54.701, eng_channel 52.010; https://t.me/CGIT_Vines/3292
2. [CGIT_Vines] Кризис среднего возраста меллиниалов выглядит именно как "а не установить ли мне ещё
   раз WOW?"… — significance 20.265, eng_channel 19.280; https://t.me/CGIT_Vines/3291
   скрыто пунктов: 34
```

Второй показанный пост — мем про WOW, к ИИ отношения не имеет. Причина та же, что в D-52: у Telegram-материалов
`classification` пуст (замер: размечены только `x` — 1 398 и `youtube` — 45 233 строки; `telegram` — 0 из
13 712 постов в окне), поэтому отличить ИИ-материал от не-ИИ нечем ни в топах платформ, ни в выборе.

## 2. Что сделать

### 2.1. Telegram-раздел в сводке не печатать

- В сводке (`build_compact`) раздел Telegram не выводится вообще: ни топы платформ, ни «Виральные».
- Блок 5 полного отчёта и раздел «Telegram — топ постов за окно» полного отчёта остаются БЕЗ изменений:
  владелец видит сырые аномалии в полном отчёте, а в короткую сводку они не идут.
- Инвариант печатать через предохранитель, а не «убрали строку»: функция сборки сводки проверяет, что в
  итоговом тексте нет ни одной Telegram-ссылки (`t.me/`), и при наличии — не печатает раздел целиком.

### 2.2. Подвал «скрыто по платформам» должен соответствовать факту

- Строка «Скрыто по платформам: …» не должна упоминать Telegram, пока Telegram в сводку не попадает.
- Освободившиеся байты бюджетом не занимать искусственно: `COMPACT_LIMIT` не менять (4 000 байт), просто
  сводка выходит короче. Добора «чем-нибудь» нет.

### 2.3. Расширить формулировку D-52 (реестр уже есть, ID не создавать заново)

- D-52 в `TECH-DEBT.md` описывает только «Виральные». Дополнить существующую строку и секцию `### D-52`
  так, чтобы суть покрывала и топы платформ сводки: Telegram-материалы в сводке не показываются вообще
  (ни топы, ни «Виральные»); вернуть 1 позицию Telegram в сводку можно только после появления
  тематической маркировки Telegram-**каналов**.
- Маркер `TODO(debt-D-52)` в коде дополнить там, где собирается сводка (раздел Telegram в `build_compact`);
  маркер, стоящий в `viral_compact_items` из ТЗ-37, сохранить (двусторонняя связка `tests/test_debt_markers.py`
  должна проходить).

## 3. Границы работы

- Менять только `tuber/analysis/report.py`, тесты и `TECH-DEBT.md`.
- Полный отчёт (все пять блоков и все его разделы) обязан остаться побайтово совпадающим с выдачей
  коммита `2f6b933` на той же копии базы (time-dependent поля нормализовать).
- Боевую базу `data/tuber.db` и `data/.lock` не трогать; чтение — `store.open_readonly`; выдача — на копии
  (`VACUUM INTO /tmp/tz38-copy.db`).
- Никаких новых зависимостей и сетевых вызовов.

## 4. Тесты (дополнить `tests/analysis/test_report_viral.py` либо `tests/analysis/test_report_compact.py`)

1. Сводка не содержит ни одной строки `t.me/` и ни одного Telegram-раздела.
2. Полный отчёт Telegram-раздел содержит (позитивная проверка, чтобы удаление не переехало в полный отчёт).
3. Подвал «Скрыто по платформам» не упоминает Telegram.
4. Предохранитель: если телеграм-раздел подсунуть в сводку искусственно (monkeypatch, возвращающий строку
   с `t.me/`), сборка его выбрасывает.
5. Сводка остальных разделов не изменилась (YouTube, X, «Виральные», сквозной сюжет — по названиям разделов).
6. `tests/test_debt_markers.py` проходит после расширения D-52.

## 5. Приёмка (фактический текст, не обещание)

1. Полный текст сводки и её размер в байтах (≤ 4 000).
2. Список разделов сводки по порядку — подтверждение, что Telegram отсутствует.
3. Отметки ЕСТЬ/НЕТ по `t.me/` в сводке и в полном отчёте (в сводке — НЕТ, в полном — ЕСТЬ) и число
   вхождений в каждом.
4. Подтверждение, что полный отчёт совпадает с выдачей `2f6b933` на той же копии базы.
5. `python3 -m pytest -q tests/` — последние строки; известное падение
   `tests/x/test_tz6.py::test_report_translates_and_caches_in_report_texts` (воспроизводится на `1a05fb6`)
   перечислить отдельно.
6. Коммит: `ТЗ-38: сводка — Telegram-раздел убран до маркировки каналов (D-52)`
