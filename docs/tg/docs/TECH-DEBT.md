# Tuber-Telegram — реестр технического долга

Заведён в ТЗ «виральность» (16.09.2026). Формат двусторонней связи: каждая запись
`D-XX` имеет маркер в коде вида `# TODO(debt-D-XX): <суть> — см. docs/TECH-DEBT.md`.
Поиск маркеров: `grep -rn "TODO(debt-D" scripts/`.

| ID | Суть долга | Где маркер | Что нужно, чтобы закрыть |
|----|-----------|-----------|--------------------------|
| D-01 | `posts.forwards` нельзя взять из веб-превью `t.me/s`: счётчика там нет (есть только просмотры и `forwarded_from`). Бэкфилл из `t.me/s` дал 0. | `scripts/collect.py` (`extract_forwards`), `scripts/scoring.py` (`backfill_forwards`) | Чтение форвардов через Telethon/MTProto (`Message.forwards`) по подписанным каналам, порциями ≤50 вызовов/прогон, с учётом `flood_until`. См. DESIGN §4.1. |
| D-02 | `Fr` в формуле DESIGN не определён однозначно → принято 1.0. | `scripts/scoring.py` (`compute_scores`) | Уточнить в DESIGN, что такое `Fr` (вероятно, доля форвардов/репостов), затем подставить реальный множитель. |
| D-03 | `TopicWeight` требует классификатора 10 тем (этап 2) → принято 1.0. | `scripts/scoring.py` (`compute_scores`) | DeepSeek-классификатор тем + `prompts/classify.md` + кэш по `text_hash`; таблица `classified` пуста. |
| D-04 | Реклама определяется keyword-эвристикой (`collect.py`: `erid`/`реклам`/`промокод`), владелец подход отверг. Расширять ключевые слова запрещено. | `scripts/collect.py` (`is_ad_text`), `scripts/scoring.py` | Семантический классификатор рекламы: DeepSeek + файл промпта `prompts/is_ad.md` + кэш ответов по `text_hash`; поле `classified.is_ai/topic` уже есть. |
| D-05 | `Xconf` считается только по точному `text_hash`; кластер simhash не построен. | `scripts/scoring.py` (`compute_scores`) | SimHash/MinHash-LSH по шинглам (DESIGN §4.5), порог на gold-сете; объединять с точным хешем в «событие». |
| D-06 | Признак агрегатора (`is_author`) по точному `text_hash` в окне 48ч даёт всего 2 агрегатора: почти-дубли (синонимы, перестановки) не ловятся. | `scripts/scoring.py` (`compute_baselines`) | Нечёткий дедуп (simhash) для `dup_ratio`, порог на gold-сете; иначе признак занижает агрегаторность. |
| D-07 | Выдача — только файл `reports/significance-*.md`; нет постера/дайджеста и записи в `story*`/`stories`. | `scripts/scoring.py` (`main`, report) | Дайджест в Telegram (Bot API, HTML) по расписанию (DESIGN §7); слой сюжетов `stories`/`story_members` пуст. |
| D-08 | `Eng` = геометрическое среднее двух баз (канал и общая медиана) — решение агента, а не DESIGN. Плюс `Xconf×Wsrc` при точных дублях почти сокращается (≈1). | `scripts/scoring.py` (`combine_eng`) | Зафиксировать схему сведения двух баз в DESIGN; решить, нужен ли `Xconf` как буст по независимым источникам сюжета, а не по точному тексту. |
| D-09 | `channel_baselines`/`scores` считаются по запросу, не в расписании; инкрементального пересчёта нет. | `scripts/scoring.py` (`connect`) | Добавить задание планировщика `tuber_telegram_score.sh` (после сбора) и инкремент по `first_seen_at`. |
| D-10 | Гейт записи в боевую БД реализован для `scoring.py`; сборщик `collect.py` пишет в боевую по умолчанию без флага (так задумано для крона). | `scripts/scoring.py` (`assert_can_write`) | Единый модуль доступа к БД с гейтом `--allow-production` для всех разовых скриптов. |
| D-11 | Пороги микровыборки `min_views`/`min_reactions` (100/5) откалиброваны по снимку боевой базы 16.09.2026 (ТЗ «порог показов»); распределение ER изменится после классификатора тем (D-03) и форвардов (D-01). | `scripts/scoring.py` (`threshold_clauses`), `config/scoring.json` (`min_views`,`min_reactions`) | Пересчитать `threshold_table()` после этапа классификатора/форвардов и перекалибровать значения; хранить снимок в `config/scoring.json:threshold_audit`. |

---

## Внимание: нумерация перенесена (ТЗ-4, 17.09.2026)

Это КОПИЯ локального реестра проекта `tuber-telegram` — исторический документ.
После переноса в монорепозиторий номера стали сквозными, чтобы не столкнуться с
долгами других платформ:

| здесь (локальный) | в монорепозитории (`/root/tuber/TECH-DEBT.md`) |
|---|---|
| D-01      | D-34 |
| D-02      | D-35 |
| D-03      | D-36 |
| D-04      | D-37 |
| D-05      | D-38 |
| D-06      | D-39 |
| D-07      | D-40 |
| D-08      | D-41 |
| D-09      | D-42 |
| D-10      | D-43 |
| D-11      | D-44 |

Актуальные тексты, маркеры `TODO(debt-D-XX)` в коде и статусы — только в
`TECH-DEBT.md` монорепозитория (`tests/youtube/test_debt_registry.py` проверяет,
что каждый маркер ссылается на существующий раздел именно там).
